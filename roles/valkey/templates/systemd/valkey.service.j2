[Unit]
Description=valkey {{ current_service.canonical }}
After=network-online.target local-fs.target
Wants=network-online.target

[Service]
Type=exec
NotifyAccess=all
Restart=always
RestartSec=5s
TimeoutStartSec=0
TimeoutStopSec=30

ExecStart=/usr/bin/podman run \
    --replace \
    --rm \
    --name vsh-{{ current_service.canonical }} \
    --volume vsh-{{ current_service.canonical }}:/data \
    --volume {{ current_service.configuration.base_path }}/config/valkey.conf:/etc/valkey/valkey.conf:Z \
    --network host \
    {{ current_service.configuration.docker_image }} valkey-server /etc/valkey/valkey.conf
ExecReload=/usr/bin/podman restart vsh-{{ current_service.canonical }}
ExecStop=/usr/bin/podman stop --time 10 vsh-{{ current_service.canonical }}

[Install]
WantedBy=default.target
