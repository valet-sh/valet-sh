##
#   Copyright 2020 TechDivision GmbH
#
#   Licensed under the Apache License, Version 2.0 (the "License");
#   you may not use this file except in compliance with the License.
#   You may obtain a copy of the License at
#
#       http://www.apache.org/licenses/LICENSE-2.0
#
#   Unless required by applicable law or agreed to in writing, software
#   distributed under the License is distributed on an "AS IS" BASIS,
#   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#   See the License for the specific language governing permissions and
#   limitations under the License.
##
---

- name: Pull rabbitmq-server {{ current_rabbitmq_service.service.version }} image
  containers.podman.podman_image:
    name: "{{ current_rabbitmq_service.configuration.docker_image }}"
    pull: true
    state: present

- name: "ensure user systemd directory exists"
  ansible.builtin.file:
    path: "{{ current_home }}/.config/systemd/user"
    state: directory
    mode: "0755"

- name: Provide systemd service for vsh-{{ current_rabbitmq_service.canonical }} container
  ansible.builtin.template:
    src: "systemd/rabbitmq-server.service.j2"
    dest: "{{ current_home }}/.config/systemd/user/vsh-{{ current_rabbitmq_service.canonical }}.service"
    owner: "{{ current_user }}"
    group: "{{ current_group }}"
    mode: "0644"
  notify: "systemd | vsh-rabbitmq-server | restarted"

- name: ensure vsh-{{ current_rabbitmq_service.canonical }} is running and enabled
  ansible.builtin.systemd:
    name: "{{ current_rabbitmq_service.service.name }}"
    state: started
    enabled: true
    scope: "{{ current_rabbitmq_service.service.scope }}"

- name: "Mark vsh-{{ current_rabbitmq_service.canonical }} as installed in the services file"
  vsh_bundle_state:
    action: "add_installed"
    canonical_names: "{{ current_rabbitmq_service.canonical }}"

- name: "Mark vsh-{{ current_rabbitmq_service.canonical }} as enabled in the services file"
  vsh_bundle_state:
    action: "set_enabled"
    canonical_names: "{{ current_rabbitmq_service.canonical }}"

- name: set 'cert_prefix' variable
  ansible.builtin.set_fact:
    cert_prefix: "rabbitmq"

- name: "generate ssl-certificate for rabbitmq.{{ development_tld }}"
  ansible.builtin.include_role:
    name: certificate
    public: true

- name: provide nginx vhost configuration for rabbitmq management plugin
  ansible.builtin.template:
    src: "rabbitmq.conf.j2"
    dest: "/etc/nginx/conf.d/rabbitmq.conf"
    owner: root
    group: root
    mode: "0644"
  notify: systemd | vsh-nginx | reloaded
  become: true

- name: unset 'cert_prefix' variable
  ansible.builtin.set_fact:
    cert_prefix: "{{ non_existent | default(None) }}"
