#!/bin/bash

AVAILABLE_VALKEY_VERSIONS='{{ valet_sh_bundle_definitions.services.valkey.versions | join(" ") }}'

function get_target_version {
    VALET_SH_FILE=$1

    SELECTED_VALKEY_VERSION=$(/usr/local/valet-sh/venv/bin/shyaml get-value services.valkey.version < "${VALET_SH_FILE}" 2>/dev/null)
   	# shellcheck disable=SC2181
    if [ $? -eq 0 ]
    then
      if [[ " $AVAILABLE_VALKEY_VERSIONS " =~ .*\ $SELECTED_VALKEY_VERSION\ .* ]]; then
         TARGET_EDITION=valkey
         TARGET_VERSION=$SELECTED_VALKEY_VERSION
         return 0
      fi
    fi
    return 1
}

if test -f ".valet-sh.yml"; then
  CURRENT_VALET_SH_FILE=".valet-sh.yml"
elif test -f "../.valet-sh.yml"; then
  CURRENT_VALET_SH_FILE="../.valet-sh.yml"
fi

if [[ $CURRENT_VALET_SH_FILE ]]
then
  get_target_version $CURRENT_VALET_SH_FILE
  # shellcheck disable=SC2181
  if [ $? -eq 0 ]
  then
    exec /usr/local/bin/"${TARGET_EDITION}${TARGET_VERSION}-cli" "$@"
  else
    exec /usr/local/bin/default-valkey-cli "$@"
  fi
else
  exec /usr/local/bin/default-valkey-cli "$@"
fi
