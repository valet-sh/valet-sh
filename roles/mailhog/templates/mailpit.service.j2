[Unit]
Description=Mailpit Service

[Service]
Type=simple
User={{ current_user }}
ExecStart={{ mailpit_bin_target }} -api-bind-addr=127.0.0.1:8025 -ui-bind-addr=127.0.0.1:8025 -smtp-bind-addr=127.0.0.1:1025
Restart=always
WorkingDirectory=/

[Install]
WantedBy=multi-user.target
