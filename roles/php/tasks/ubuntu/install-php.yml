##
#   Copyright 2020 TechDivision GmbH
#
#   Licensed under the Apache License, Version 2.0 (the "License");
#   you may not use this file except in compliance with the License.
#   You may obtain a copy of the License at
#
#       http://www.apache.org/licenses/LICENSE-2.0
#
#   Unless required by applicable law or agreed to in writing, software
#   distributed under the License is distributed on an "AS IS" BASIS,
#   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#   See the License for the specific language governing permissions and
#   limitations under the License.
##
---

- ansible.builtin.include_tasks: "migrations/main.yml"

- name: "set handler_current_service fact"
  ansible.builtin.set_fact:
    handler_current_service: "{{ current_service }}"

- name: ensure apt-transport-https is installed
  apt:
    name: apt-transport-https
    state: present
  become: true

- name: "download and install https://packages.sury.org/debsuryorg-archive-keyring.deb"
  ansible.builtin.apt:
    deb: https://packages.sury.org/debsuryorg-archive-keyring.deb
  become: true

- name: "Ensure php repository is present in sources.list.d'"
  ansible.builtin.copy:
    content: |
      deb [signed-by=/usr/share/keyrings/debsuryorg-archive-keyring.gpg] https://packages.sury.org/php/ {{ ansible_facts['distribution_release'] }} main
    dest: /etc/apt/sources.list.d/php.list
    mode: '0644'
  become: true

- name: Ensure '/etc/apt/sources.list.d/ppa_ondrej_php_noble.list' is absent
  file:
    path: "/etc/apt/sources.list.d/ppa_ondrej_php_noble.list"
    state: absent
  become: true

- name: update apt caches
  apt:
    update_cache: yes
  ignore_errors: True
  become: true

- name: fail when php version is not set
  ansible.builtin.fail:
    msg: "version variable is not defined"
  when: (current_service.version is not defined) or (current_service.version == "")

- name: set php_version variable
  ansible.builtin.set_fact:
    php_version: "{{ current_service.version }}"

- name: debug current_service
  ansible.builtin.debug:
    var: current_service

- name: "override default configuration for current php"
  ansible.builtin.include_tasks: override-config.yml

- name: create specific php module list
  ansible.builtin.set_fact:
    php_specific_modules: "{{ php_modules | map('regex_replace', '^(.*)$', php_version_string + '-\\1') | list }}"

- name: Debug php_specific_modules
  ansible.builtin.debug:
    var: php_specific_modules

- name: ensure xdebug.ini configuration is deleted
  ansible.builtin.file:
    path: "/etc/php/{{ php_version }}/mods-available/xdebug.ini"
    state: absent
  become: true

- name: ensure xdebug php package can be installed
  ansible.builtin.file:
    path: "/etc/apt/preferences.d/valet-sh-php{{ php_version | replace('.', '-') }}-xdebug"
    state: absent
  become: true


- name: Debug php_specific_modules
  ansible.builtin.debug:
    var: php_specific_modules

- name: "install {{ php_version_string }} modules"
  ansible.builtin.apt:
    name: "{{ php_specific_modules }}"
    state: present
  notify: "systemd | vsh-php | restarted"
  become: true

- name: "include xdebug compilation tasks"
  ansible.builtin.include_tasks: compile-xdebug.yml
  when:
    - current_service.compile_xdebug is defined
    - current_service.compile_xdebug.status is defined
    - current_service.compile_xdebug.status|bool

- name: get stat for xdebug cli configuration file
  ansible.builtin.stat:
    path: "/etc/php/{{ php_version }}/cli/conf.d/20-xdebug.ini"
  register: php_xdebug_cli_config_obj

- name: get stat for xdebug fpm configuration file
  ansible.builtin.stat:
    path: "/etc/php/{{ php_version }}/fpm/conf.d/20-xdebug.ini"
  register: php_xdebug_fpm_config_obj

- name: delete xdebug cli configuration symlink if exists
  ansible.builtin.file:
    path: "/etc/php/{{ php_version }}/cli/conf.d/20-xdebug.ini"
    state: absent
  when: php_xdebug_cli_config_obj.stat is defined and php_xdebug_cli_config_obj.stat.islnk is defined
  become: true

- name: delete xdebug fpm configuration symlink if exists
  ansible.builtin.file:
    path: "/etc/php/{{ php_version }}/fpm/conf.d/20-xdebug.ini"
    state: absent
  when: php_xdebug_fpm_config_obj.stat is defined and php_xdebug_fpm_config_obj.stat.islnk is defined
  notify: "systemd | vsh-php | restarted"
  become: true

- name: get stat for error.log file
  ansible.builtin.stat:
    path: "{{ php_error_log_path }}"
  register: php_error_log_file_obj

- name: create php_errors.log file
  ansible.builtin.file:
    dest: "{{ php_error_log_path }}"
    owner: "{{ current_user }}"
    group: "{{ current_group }}"
    state: "{{ 'file' if php_error_log_file_obj.stat.exists else 'touch' }}"
    mode: "0755"
  become: true

- name: "ensure {{ php_version_string }} cli settings are up2date"
  ansible.builtin.copy:
    content: "{{ php_cli_settings }}"
    dest: "{{ php_cli_conf_path }}"
    owner: root
    group: root
    mode: "0644"
  become: true

- name: "ensure {{ php_version_string }} fpm settings are up2date"
  ansible.builtin.copy:
    content: "{{ php_fpm_settings }}"
    dest: "{{ php_fpm_conf_path }}"
    owner: root
    group: root
    mode: "0644"
  notify: "systemd | vsh-php | restarted"
  become: true

- name: set config setting in php pool configuration
  ansible.builtin.lineinfile:
    path: "{{ php_fpm_pool_config }}"
    regexp: "^{{ inside_item.conf }}="
    line: "{{ inside_item.conf }}={{ inside_item.value }}"
  with_items: "{{ php_fpm_pool_configuration }}"
  loop_control:
    loop_var: inside_item
  notify: "systemd | vsh-php | restarted"
  become: true

- name: ensure xdebug2 config is deployed
  ansible.builtin.template:
    src: "ubuntu/xdebug2.ini.j2"
    dest: "/etc/php/{{ php_version }}/mods-available/xdebug2.ini"
    owner: root
    group: root
    mode: "0644"
  become: true
  when: "php_version in native_xdebug2_versions"

- name: ensure xdebug3 config is deployed
  ansible.builtin.template:
    src: "ubuntu/xdebug3.ini.j2"
    dest: "/etc/php/{{ php_version }}/mods-available/xdebug3.ini"
    owner: root
    group: root
    mode: "0644"
  become: true
  when: "php_version in native_xdebug3_versions"

- name: ensure compiled xdebug2 config is deployed
  ansible.builtin.template:
    src: "ubuntu/xdebug2-compiled.ini.j2"
    dest: "/etc/php/{{ php_version }}/mods-available/xdebug2.ini"
    owner: root
    group: root
    mode: "0644"
  become: true
  when: "php_version in compiled_xdebug2_versions"

#- name: "set default php cli version"
#  community.general.alternatives:
#    name: php
#    path: "/usr/bin/php{{ current_service_item.version }}"
#  with_items: "{{ current_service }}"
#  become: true
#  loop_control:
#    loop_var: current_service_item
#  when:
#    - current_service_item.defaultable is defined
#    - current_service_item.defaultable|bool

- name: "Flush handlers"
  ansible.builtin.meta: flush_handlers

- name: "Get desired state of {{ current_service.service.name }} from services file"
  vsh_bundle_service_state_info:
    name: "{{ current_service.canonical }}"
  register: current_service_state

- name: "If desired state is unknown, set it to enabled"
  vsh_bundle_state:
    action: "initialize_empty"
    canonical_names: "{{ current_service.canonical }}"
  when: current_service_state.state == "unknown"

- name: "Update current_service_state after initialization"
  ansible.builtin.set_fact:
    current_service_state: "{{ current_service_state | combine({'state': 'enabled'}) }}"
  when: current_service_state.state == "unknown"

- name: "Set {{ current_service.service.name }} container state"
  ansible.builtin.systemd:
    name: "{{ current_service.service.name }}"
    state: "{{ 'started' if current_service_state.state == 'enabled' else 'stopped' }}"
    enabled: "{{ 'true' if current_service_state.state == 'enabled' else 'false' }}"
    scope: "{{ current_service.service.scope }}"

- name: "Mark {{ current_service.service.name }} as installed in the services file"
  vsh_bundle_state:
    action: "installed"
    canonical_names: "{{ current_service.canonical }}"
