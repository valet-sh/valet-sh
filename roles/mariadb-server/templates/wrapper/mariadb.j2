#!/bin/bash

{% if current_mariadb_key|float == 11.4 %}
exec podman exec -it {{ current_mariadb_key }} mysql --defaults-file=/etc/mysql/my.cnf "$@"
{% else %}
exec podman exec -it {{ current_mariadb_key }} mariadb --defaults-file=/etc/mysql/my.cnf "$@"
{% endif %}
