#!/usr/bin/env ansible-playbook
---
#
# valet.sh
#
# @author: "Johann Zelger"
# @command: "install"
# @description: "Install packages"
# @subcommands: ["all","php","nginx","redis"]

- name: "distil:install"
  hosts: local
  gather_facts: True

  roles:
    - gnutar
    - redis
    - php
